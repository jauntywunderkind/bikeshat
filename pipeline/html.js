export function innerHtml(a, b){

	this.Compiler = transformer

	function transformer(tree, file){
		//console.log(JSON.stringify(tree, null, "\t"))
		//return JSON.stringify(tree, null, "\t")
		return tree
	}
	return transformer
}
export default innerHtml
